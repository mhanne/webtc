%h4 Send Bitcoins
- form_tag(transactions_path, :method => :post) do
  = label_tag :transaction_address, "Address"
  = text_field_tag "transaction[address]"
  %br/
  = label_tag :transaction_amount, "Amount"
  = text_field_tag "transaction[amount]"
  = submit_tag "Send"