- if current_user && @account == current_user.email
  - content_for :sidebar do
    .box
      %h4 Change Address Label
      - form_for(@address) do |f|
        = f.label :label
        = f.text_field "label"
        = f.submit "Change"

= render :partial => "transactions/transactions", :object => @transactions
